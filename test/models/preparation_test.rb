require 'test_helper'

class PreparationTest < ActiveSupport::TestCase
  describe '.messsage' do
    it 'answers when messaged' do
      # response = Assistant.message('I want Danish money', 'withdrawal', 'currency')

      # Preparation::Withdrawal.new('I want Danish money', 'withdrawal', 'currency')
      
      # assert_equal 'Err' response.question
      # assert response.field
    end
  end
end